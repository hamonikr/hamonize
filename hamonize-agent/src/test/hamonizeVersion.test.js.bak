// import {hamonizeVersion} from "../main";
test('1 + 1 은 2일겁니다.', () => {
    expect(1 + 1).toEqual(2);
});

//jest.mock("child_process")

 describe('Test main', () => {
    const hamonizeVersion = require('./hamonizeVersion');
    test('hamonizeVersion function', async() => {
        expect(hamonizeVersion()).toEqual("1.1.1");
    });
 });